import { RegionTypeCombinationPipe } from './region-type-combination.pipe';

describe('RegionTypeCombinationPipe', () => {
  it('create an instance', () => {
    const pipe = new RegionTypeCombinationPipe();
    expect(pipe).toBeTruthy();
  });
});
